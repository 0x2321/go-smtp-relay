[Unit]
Description=Lightweight local SMTP relay that forwards messages to an upstream SMTP server
After=network.target

[Service]
User=root
Group=root
ExecStart=/bin/smtp-relay
Type=notify-reload
Restart=always

[Install]
WantedBy=default.target